import { Header } from "@/widgets/Header"
import { Box } from "@mantine/core"

export const MainPage = () => {
    return (
        <Box mih="100vh">
            <Header></Header>
        </Box>
    )
}